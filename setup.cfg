[metadata]
name = simd-blend-modes
version = 0.0.0
description = SIMD-accelerated blend modes
license = MIT

[options]
package_dir =
    = src
packages = find:
python_requires = >=3.9
install_requires =
    numpy

[options.packages.find]
where = src
